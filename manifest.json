{
    "manifest_version": 3,
    "name": "Get Discord Button Source (OnClick)",
    "version": "1.1",
    "description": "Extracts the src attribute of elements with class 'button button-join is-discord' when the extension icon is clicked.",
    "permissions": [
      "activeTab",
      "scripting" 
    ],
    "action": { 
      "default_popup": "" 
    },
    "background": {
      "service_worker": "background.js",
      "scripts": ["background.js"], 
      "persistent": false
    }
  }